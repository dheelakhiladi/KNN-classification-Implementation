import numpy as np
import copy as cp
def distance(a,b,x) :
	return dist = numpy.linalg.norm(a-b,axis = x)

A=['1','2','4','5','1','3','7']

k=[2,7,5]
error = 1
while error ! = 0
	for i in range(len(A)):
		distances = distance(A[i],k)
		cluster[i] = distance.index(min(distance))
	k_old = cp.deepcopy(k)

	for i in range(len(k)):
		dp = np.array(X[j]) for j in range(len(A) if clusters[j] == i)
		k[i] = np.mean(dp)
	
	error= dist(k, k_old, None)

print (A)
print (clusters)



